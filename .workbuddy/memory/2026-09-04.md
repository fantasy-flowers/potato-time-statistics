## 2026-09-04
- 修复周/月维度卡片被长排行列表撑高：BuildMainContent root 定高 600（两卡同高对齐、比旧版~494更大），chartHost 去掉写死430改为随卡拉伸，右侧排行列表高度受限后 ScrollViewer 自动滚动；日维度不受影响；x64 Debug 编译通过（0警告0错误）
- 插件名称由"游戏统计"改为"统计"（Plugin.cs Info.Name），x64 Debug 编译通过
- 侧边栏文字与页面大标题由"游戏统计"改为"统计"：改了 Strings 三语言文件的 Sidebar_Text/Page_Title + Plugin_Ui.cs fallback；Release 重打包并覆盖部署到宿主 _PluginXamlHotReload 目录，需重启 PotatoVN 生效
- 游戏分布卡重构为左图右内容（定高420、等宽两列）：DonutChart 泛化出通用入口 DonutDatum{Id?,Name,Value,Icon?}（日维度旧入口保留为包装）；新建 TreemapChart 控件（squarified 算法 + Canvas 绝对定位，格子≥70×40显示名称+数量、≥36×20只显示数量、否则tooltip兜底）；BuildDistributionCard 重写——状态=环形图+图例行，引擎/公司=树图（Top12+其他聚合）+排行式明细（序号+色块+名称+数量/占比+占比条）；Release 构建0警告0错误，已覆盖部署到热加载目录，重启宿主生效
- 修复改日期/月份后筛选不重置：日维度选游戏（_selectedGameId）或周/月维度点柱形（_selectedIndex）后改日期，筛选残留到新时段。抽 ClearChartSelection() 统一清两个字段，在日历选日、月份面板选月、今天/本月按钮三处入口调用（OnPeriodSelected 也改为复用）；Release 0警告0错误，已重打包并部署 dll/pdb/deps.json 到宿主热加载目录（.pri 被运行中宿主锁占用未覆盖，但纯 C# 改动 .pri 内容未变，无影响），重启宿主生效
